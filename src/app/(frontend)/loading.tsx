import LoadingMask from '@/components/LoadingMask'

/**
 * Loading fallback component for route segments.
 * Displays a loading mask while content is being fetched.
 */

export default function Loading() {
  return <LoadingMask />
}
